{
    "name": "llm-hcfy",
    "private": true,
    "type": "module",
    "scripts": {
        "sync:config": "node scripts/sync-shared-config.mjs",
        "check:config": "node scripts/sync-shared-config.mjs --check",
        "validate:config": "node scripts/validate-shared-config.mjs",
        "test": "node --test tests/*.test.mjs"
    }
}
